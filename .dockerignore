# VCS / editor
.git
.gitignore
.vscode
.claude
.idea
README.md

# Dependencies and build output — rebuilt inside the image
node_modules
.next
out
build
coverage

# Local tooling noise
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo
.DS_Store
.vercel

# Docker's own files
Dockerfile
.dockerignore
docker-compose*.yml

# NOTE: .env is deliberately NOT ignored. NEXT_PUBLIC_* values must be present
# at build time so Next can inline them into the client bundle.
